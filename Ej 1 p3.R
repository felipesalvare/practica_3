
# Ej 1 p3 -----------------------------------------------------------------
rm(list=ls())

# a) ----------------------------------------------------------------------

a <- c(1:1000)

b <- c()
for (i in 1:1000) {
  b <- c(b,i)
}

# b) ----------------------------------------------------------------------
rm(list = ls())
i <- 0
j <- 0
m <- matrix(1:600,nrow = 30,ncol = 20)
for ( i in 1:30 ) {
  for (j in 1:20 ) {
    m[i,j] = i * j
    j = j + 1
  } 
}

# c) ----------------------------------------------------------------------

i <- 1:30
j <- 1:20
m1 <- matrix(i, nrow=30, ncol=20, byrow = FALSE)
m2 <- matrix(j, nrow = 30, ncol=20, byrow = TRUE )
mm <- m1 * m2

# d) ----------------------------------------------------------------------

e <- mm[1:30,5]

# e) ----------------------------------------------------------------------

rm(list = ls())

m <- array(data= c(1:600), dim = c(30,20,10))
i <- 0
j <- 0
k <- 0
for ( i in 1:30 ) {
  for (j in 1:20 ) {
    for (k in 1:10 ) {
      m[i,j,k] = i * j * k
    }
  }
}
print(m)

k2 <- array(m[,,2],dim=c(30,20))
